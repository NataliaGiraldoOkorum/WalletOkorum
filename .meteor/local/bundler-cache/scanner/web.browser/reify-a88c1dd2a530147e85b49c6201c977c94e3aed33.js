let React;module.link('react',{default(v){React=v}},0);
var ThemeContext = React.createContext(null);

if (process.env.NODE_ENV !== 'production') {
  ThemeContext.displayName = 'ThemeContext';
}

module.exportDefault(ThemeContext);